---
phase: 02-python-integration-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - renderer/__init__.py
  - renderer/protocol.py
  - renderer/models.py
  - renderer/remotion.py
  - config.py
  - requirements.txt
  - pyproject.toml
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_renderer_protocol.py
autonomous: true
requirements: [INTG-02]

must_haves:
  truths:
    - "VideoRendererProtocol defines render(), get_status(), and health_check() methods"
    - "RemotionRenderer class satisfies VideoRendererProtocol via isinstance() check"
    - "RenderRequest and JobStatus Pydantic models validate correctly"
    - "pytest test infrastructure runs and test stubs execute"
  artifacts:
    - path: "renderer/protocol.py"
      provides: "VideoRendererProtocol with @runtime_checkable"
      contains: "class VideoRendererProtocol"
    - path: "renderer/models.py"
      provides: "RenderRequest and JobStatus Pydantic models"
      exports: ["RenderRequest", "JobStatus"]
    - path: "renderer/remotion.py"
      provides: "RemotionRenderer skeleton implementing protocol"
      contains: "class RemotionRenderer"
    - path: "renderer/__init__.py"
      provides: "Package exports"
      exports: ["VideoRendererProtocol", "RemotionRenderer", "RenderRequest", "JobStatus"]
    - path: "tests/conftest.py"
      provides: "Shared test fixtures"
    - path: "tests/test_renderer_protocol.py"
      provides: "Protocol compliance tests"
  key_links:
    - from: "renderer/remotion.py"
      to: "renderer/protocol.py"
      via: "structural typing — RemotionRenderer satisfies VideoRendererProtocol"
      pattern: "isinstance.*VideoRendererProtocol"
    - from: "renderer/remotion.py"
      to: "renderer/models.py"
      via: "uses RenderRequest and JobStatus types"
      pattern: "from renderer.models import"
---

<objective>
Define the VideoRendererProtocol abstraction, Pydantic data models, RemotionRenderer skeleton, and test infrastructure — the contracts that all subsequent plans build against.

Purpose: Establishes the protocol-based architecture (INTG-02) so the renderer engine can be swapped without changing calling code. Also installs pytest and creates the test scaffold needed for Wave 0 compliance.
Output: renderer/ package with protocol, models, skeleton implementation; tests/ directory with fixtures and protocol tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-python-integration-layer/02-CONTEXT.md
@.planning/phases/02-python-integration-layer/02-RESEARCH.md
@.planning/phases/02-python-integration-layer/02-VALIDATION.md

@config.py
@requirements.txt

<interfaces>
<!-- Remotion service HTTP contract (Phase 1) — the API that RemotionRenderer will call -->

From remotion-service/remotion/schemas.ts:
```typescript
export const ReelInputSchema = z.object({
  sourceVideoUrl: z.string(),
  hookText: z.string(),
  bodyText: z.string(),
  textDirection: z.enum(["rtl", "ltr"]).default("rtl"),
  animationStyle: z.enum(["fade", "slide"]).default("fade"),
  durationInSeconds: z.number().min(3).max(90).default(15),
  sourceVideoLocalPath: z.string().optional(),  // Injected by render-queue, not HTTP
});
```

From remotion-service/server/index.ts:
```typescript
// POST /renders — accepts ReelInputSchema fields, returns { jobId: string }
// GET /renders/:id — returns JobState { state, progress, videoUrl?, error? }
// GET /health — returns { status: "ok" }
```

From config.py:
```python
SUPABASE_URL = os.getenv("SUPABASE_URL", "")
SUPABASE_KEY = os.getenv("SUPABASE_KEY", "")
SUPABASE_BUCKET = os.getenv("SUPABASE_BUCKET", "rendered-videos")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install test infrastructure and create renderer package with protocol + models</name>
  <files>requirements.txt, pyproject.toml, renderer/__init__.py, renderer/protocol.py, renderer/models.py, config.py, tests/__init__.py, tests/conftest.py, tests/test_renderer_protocol.py</files>
  <behavior>
    - Test: RenderRequest validates with all required fields (source_video_url, hook_text, body_text, record_id)
    - Test: RenderRequest rejects missing required fields
    - Test: RenderRequest has sensible defaults for optional fields (text_direction="rtl", animation_style="fade", duration_in_seconds=15)
    - Test: JobStatus accepts all valid state values ("accepted", "rendering", "downloading", "uploading", "completed", "failed", "timed_out")
    - Test: JobStatus.video_url is optional (None when not completed)
  </behavior>
  <action>
    1. Install test dependencies: `pip install pytest pytest-asyncio` and add them to requirements.txt (pytest>=8.0.0, pytest-asyncio>=0.25.0).

    2. Create pyproject.toml with pytest config:
       ```toml
       [tool.pytest.ini_options]
       asyncio_mode = "auto"
       testpaths = ["tests"]
       ```

    3. Create tests/__init__.py (empty) and tests/conftest.py with:
       - A `mock_renderer` fixture that returns a mock object satisfying VideoRendererProtocol (use unittest.mock.AsyncMock for render, get_status, health_check methods)
       - A `sample_render_request` fixture returning a valid RenderRequest instance

    4. Create renderer/__init__.py exporting: VideoRendererProtocol, RenderRequest, JobStatus (RemotionRenderer added in next task)

    5. Create renderer/protocol.py:
       - Import Protocol, runtime_checkable from typing
       - Define VideoRendererProtocol with @runtime_checkable:
         - `async def render(self, request: RenderRequest) -> str` — submit render, return job_id
         - `async def get_status(self, job_id: str) -> JobStatus` — poll status
         - `async def health_check(self) -> bool` — check service reachable
         - `async def download_file(self, job_id: str, dest_path: str) -> None` — download rendered file

    6. Create renderer/models.py with Pydantic v2 models:
       - `RenderRequest`: source_video_url (str), hook_text (str), body_text (str), record_id (str), text_direction (Literal["rtl","ltr"] default "rtl"), animation_style (Literal["fade","slide"] default "fade"), duration_in_seconds (int, ge=3, le=90, default=15), callback_url (str | None, default None)
       - `JobStatus`: state (Literal["accepted","rendering","downloading","uploading","completed","failed","timed_out"]), progress (float, default 0.0), video_url (str | None, default None), error (str | None, default None)

    7. Add to config.py:
       - `REMOTION_SERVICE_URL = os.getenv("REMOTION_SERVICE_URL", "http://localhost:3000")`
       - `SUPABASE_SOURCE_BUCKET = os.getenv("SUPABASE_SOURCE_BUCKET", "source-videos")`

    8. Create tests/test_renderer_protocol.py with tests for model validation (behavior items above).
  </action>
  <verify>
    <automated>cd /Users/openclaw/wandi-agent && python -m pytest tests/test_renderer_protocol.py -x -v</automated>
  </verify>
  <done>pytest runs, all model validation tests pass, renderer package importable with protocol and models</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create RemotionRenderer skeleton and protocol compliance tests</name>
  <files>renderer/remotion.py, renderer/__init__.py, tests/test_renderer_protocol.py</files>
  <behavior>
    - Test: RemotionRenderer satisfies VideoRendererProtocol via isinstance() check (runtime_checkable)
    - Test: A mock class with matching method signatures also satisfies VideoRendererProtocol (proves structural typing works — swap engine needs zero code changes)
    - Test: RemotionRenderer.__init__ accepts optional base_url, defaults to config.REMOTION_SERVICE_URL
  </behavior>
  <action>
    1. Create renderer/remotion.py with RemotionRenderer class:
       - __init__(self, base_url: str | None = None): sets self.base_url from param or config.REMOTION_SERVICE_URL
       - async def render(self, request: RenderRequest) -> str: skeleton that builds payload dict (sourceVideoUrl, hookText, bodyText, textDirection, animationStyle, durationInSeconds), POSTs to {base_url}/renders via httpx.AsyncClient(timeout=10), returns resp.json()["jobId"]
       - async def get_status(self, job_id: str) -> JobStatus: GETs {base_url}/renders/{job_id}, maps response to JobStatus (state mapping: "queued"->"accepted", "in-progress"->"rendering", "completed"->"completed", "failed"->"failed")
       - async def health_check(self) -> bool: GETs {base_url}/health, returns True if 200, False on any exception
       - async def download_file(self, job_id: str, dest_path: str) -> None: streams GET {base_url}/renders/{job_id}/file to dest_path using httpx.AsyncClient(timeout=120) with iter_bytes(chunk_size=8192)

    2. Update renderer/__init__.py to also export RemotionRenderer and a factory function:
       ```python
       def get_renderer() -> VideoRendererProtocol:
           return RemotionRenderer()
       ```

    3. Add protocol compliance tests to tests/test_renderer_protocol.py:
       - test_remotion_implements_protocol: assert isinstance(RemotionRenderer(), VideoRendererProtocol)
       - test_protocol_swappability: create a DummyRenderer class with same method signatures (no inheritance), assert isinstance(DummyRenderer(), VideoRendererProtocol)
       - test_remotion_default_base_url: assert RemotionRenderer().base_url == config.REMOTION_SERVICE_URL
       - test_remotion_custom_base_url: assert RemotionRenderer("http://custom:9999").base_url == "http://custom:9999"
  </action>
  <verify>
    <automated>cd /Users/openclaw/wandi-agent && python -m pytest tests/test_renderer_protocol.py -x -v</automated>
  </verify>
  <done>RemotionRenderer passes isinstance(r, VideoRendererProtocol) check, DummyRenderer also passes (proving structural typing), all tests green</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -x -v` — all tests pass
- `python -c "from renderer import VideoRendererProtocol, RemotionRenderer, RenderRequest, JobStatus; print('imports ok')"` — package importable
- `python -c "from renderer import RemotionRenderer, VideoRendererProtocol; assert isinstance(RemotionRenderer(), VideoRendererProtocol); print('protocol ok')"` — isinstance check passes
</verification>

<success_criteria>
- renderer/ package exists with protocol.py, models.py, remotion.py, __init__.py
- VideoRendererProtocol is @runtime_checkable with 4 async methods
- RenderRequest and JobStatus are valid Pydantic v2 models
- RemotionRenderer satisfies VideoRendererProtocol (isinstance check passes)
- pytest infrastructure installed and configured (pyproject.toml)
- All protocol and model tests pass
- config.py has REMOTION_SERVICE_URL and SUPABASE_SOURCE_BUCKET
</success_criteria>

<output>
After completion, create `.planning/phases/02-python-integration-layer/02-01-SUMMARY.md`
</output>
